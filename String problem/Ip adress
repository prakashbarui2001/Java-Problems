class Solution {
    public String defangIPaddr(String a) {
        
        String str = "";
      for(int i=0;i<a.length();i++)
      {
        if(a.charAt(i)=='.')
          str+="[.]";
        else
          str+=a.charAt(i);
      }
      return str;
    }
}
